{
  "name": "minecraft-clone",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node scripts/smoke-test.mjs && node scripts/physics-test.mjs && node scripts/gameplay-test.mjs",
    "test:browser": "node scripts/browser-test.mjs"
  },
  "dependencies": {
    "three": "^0.170.0"
  },
  "devDependencies": {
    "puppeteer-core": "^24.43.1",
    "vite": "^6.0.0"
  }
}
